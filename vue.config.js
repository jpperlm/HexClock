module.exports = {
  publicPath: process.env.NODE_ENV === 'production' ? '/HexClock/' : '/',
  // where to output built files
  outputDir: 'dist'

  // where to put static assets (js/css/img/font/...)
  // assetsDir: '',
}
